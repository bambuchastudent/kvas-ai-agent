# Verify live release 1.0.5

This pull request checks all 10 PDF assets in the GitHub Release and all 10 matching multilingual pages through the published `gh-pages` branch mirror.

The native GitHub Pages domain is checked separately when enabled in repository settings.
