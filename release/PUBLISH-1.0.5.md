# Publication 1.0.5

The release pipeline must publish:

- 10 PDF files: summary and AI-agent instructions in `ru`, `en`, `es`, `de`, and `zh-CN`;
- matching web pages generated from the same rendered source;
- publication and website ZIP/TAR bundles;
- GitHub Release `v1.0.5`;
- a static `gh-pages` branch and a GitHub Pages Actions deployment attempt.

Pipeline: `.github/workflows/publish.yml`.
